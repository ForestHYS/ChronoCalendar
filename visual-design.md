# 视觉设计风格说明

本文档与 `design.md` 功能设计配套，供 UI 与开发对齐。**总体方向：极简、浅色优先、信息清晰、少装饰。**

---

## 1. 设计原则

- **极简**：每屏一个主目标；能合并的入口不拆成多个按钮；装饰性图形尽量少。
- **浅色优先**：默认主题为浅色；深色模式可作为二期，配色需单独定义（见 §7）。
- **可读优先**：正文对比度充足；统计数字与标签一眼可扫。
- **触感克制**：交互以轻微反馈为主（短动画、轻 pressed 态），避免浮夸动效。
- **与系统协调**：遵循 **Material 3** 基础规范（形状、动效时长量级），在 token 上保持品牌一致性即可。

---

## 2. 色彩系统（浅色主题）

### 2.1 品牌与表面

| Token | 用途 | 建议值（可随实现微调） |
|--------|------|------------------------|
| `color.primary` | 主操作、选中 Tab、关键链接 | `#2563EB`（冷静蓝，偏工具感） |
| `color.onPrimary` | 主按钮上的文字/图标 | `#FFFFFF` |
| `color.primaryContainer` | 弱强调背景（如选中 chip） | `#DBEAFE` |
| `color.surface` | 页面主背景 | `#FAFAFA` |
| `color.surfaceContainer` | 卡片、列表行背景 | `#FFFFFF` |
| `color.surfaceContainerHigh` | 底栏、浮层底 | `#F4F4F5` |

### 2.2 文字

| Token | 用途 | 建议值 |
|--------|------|--------|
| `color.onSurface` | 主标题、任务标题 | `#18181B` |
| `color.onSurfaceVariant` | 次要说明、时间、辅助统计 | `#71717A` |
| `color.outline` | 分割线、边框 | `#E4E4E7` |

### 2.3 语义色（克制使用）

| Token | 用途 | 建议值 |
|--------|------|--------|
| `color.success` | 已完成 | `#16A34A` |
| `color.warning` | 即将到期、稍后 | `#CA8A04` |
| `color.error` | 删除、超时、校验错误 | `#DC2626` |

统计柱状图：**按标签分配柔和色带**（如蓝、青、紫、琥珀、玫瑰），饱和度中等、彼此可区分；图例与标签 chip 颜色尽量一致。

---

## 3. 字体与排版

- **字体**：使用系统默认（Roboto / 思源黑体等），不引入额外品牌字体，保持极简与包体小。
- **字号层级（sp，参考）**
  - 页面标题：`22sp`，SemiBold
  - 任务标题 / 卡片主文案：`16sp`，Medium
  - 正文与列表次要信息：`14sp`，Regular
  - 辅助 / Tab 标签：`12sp`～`13sp`，Regular
  - 首页统计大字（本周完成数）：`40sp`～`48sp`，Bold，行高略紧
- **数字**：统计与时长使用 **tabular lining** 感（等宽数字若系统支持则开启），避免柱状图坐标跳动。
- **行距**：正文 `1.4`～`1.5`；标题略紧。

---

## 4. 形状、间距与层次

- **圆角**
  - 卡片 / 列表行容器：`12dp`
  - 按钮、输入框、Chip：`10dp`～`12dp`
  - 底栏中间「+」悬浮按钮：圆形 `28dp`～`32dp` 半径（突出但不过大）
- **间距**：**8dp 栅格**；模块之间 `16dp`～`24dp`；列表行内边距水平 `16dp`、垂直 `12dp`～`14dp`。
- **阴影**：浅色主题**尽量少用阴影**；卡片可用极轻 elevation（或仅用 `outline` 1dp 描边二选一，全应用统一）。
- **分割线**：优先用 `12dp`～`16dp` 的留白分组；必要时 `1dp` `color.outline`。

---

## 5. 图标与插图

- **图标**：**线性图标**，线宽统一（约 `1.5dp`～`2dp`），风格与 Material Symbols Outlined 一致。
- **插图**：空状态可用**极简线面结合**小图（单色或双色），避免复杂插画；无图时仅用文案 + 主按钮亦可。

---

## 6. 动效与反馈

- **时长**：页面切换 `200ms`～`300ms`；微交互（按下、展开）`100ms`～`200ms`。
- **曲线**：标准 `ease-out`；日历视图切换可用略柔和的 `cubic-bezier`。
- **原则**：动画服务于**空间位置理解**（如日历周↔月），不为炫技。

---

## 7. 深色模式（二期可选）

- 若启用：**不采用纯黑背景**，使用 `#121212`～`#18181B` 级表面，卡片略提亮一级，避免与 OLED 纯黑混淆（亦可再议）。
- **主色**：可适当提高亮度或略降饱和，保证对比度。
- **柱状图**：色带略提亮，保证在深色表面上可辨。

---

## 8. 关键界面要点（与功能稿对应）

- **底栏**：背景 `surfaceContainerHigh`，图标默认 `onSurfaceVariant`，选中 `primary`；中间「+」用 `primary` 填充 + `onPrimary` 图标。
- **任务条**：左：标题 + 时间/标签；右：文字按钮或图标按钮（「专注」「完成」），**次要操作为 outline 或 text 样式**。
- **筛选侧栏**：窄栏仅类型 + 标签多选，背景与主列表区分一级即可。
- **番茄钟页**：全屏或近全屏**单一计时数字** + 一个主按钮（结束）；白噪音为次要行内图标或底部小条，不抢视觉焦点。
- **提醒半屏弹**：白/浅灰底、顶角 `16dp` 圆角；操作纵向列表或主次双按钮，**延期/稍后**为次要样式。

---

## 9. 无障碍与触控

- 可点击区域不低于 **48×48dp**（含 padding）。
- 主文字与背景对比度建议满足 **WCAG AA**（正文约 4.5:1）。
- 不单独依赖颜色表达状态（完成可配合图标或删除线等，与团队取舍一致即可）。

---

## 10. 交付物建议（给设计落地）

- Figma / 设计工具：**Color styles、Text styles、Effect（若有）** 与上表 token 命名对齐。
- 导出：**浅色主题一版即可**先行；深色二期再补。
