# 日程管理软件 — 中期汇报

本项目目标为实现一个

> 文档说明：本文档按中期汇报要求组织；**第一部分界面/原型图按要求暂时留白**，后续可替换为原型工具导出图或真机/模拟器运行截图。

---

## 一、项目主要界面设计（原型 / 截图）

**（本部分暂时留白，待补充。）**

建议后续补充方式（任选其一或组合）：

- 使用原型设计工具（如 Figma / Axure 等）输出：登录、主页、任务列表、日历、设置、任务详情/编辑、番茄钟、AI 助手对话等关键流程线框与交互说明；
- 或直接贴入 **Flutter 应用运行截图**（含底部导航、任务卡片、Agent 顶栏入口、审批卡片等）。

---

## 二、项目关键页面实现方案

### 2.1 核心关键页面

| 页面 / 模块 | 设计意图（摘要） | 当前实现情况 | 未完成部分与计划方案 |
|-------------|------------------|----------------|----------------------|
| 登录 / 注册 | 进入应用前鉴权 | **已实现**：`LoginPage`、`RegisterPage`，JWT 与 `ApiClient` 统一请求 | - |
| 主页（最近任务 + 统计 + Todo） | 今日/明日任务、统计、Todo 展开 | **已实现**：`HomePage` 含最近任务、统计卡片、Todo 展开与专注/完成操作；数据来自 `TaskRepository` | 部分数据显示bug有待修复 |
| 任务列表 | 筛选/排序/侧栏类型/搜索 | **尚未实现** | 根据原型设计完成页面实现；接入 `GET /tasks/` 筛选参数、侧边类型栏、搜索与列表卡片
| 日历 | 日/周/月视图与任务联动 | **尚未实现**：`CalendarPage` 占位 | 根据原型设计完成页面实现，并实现切换动画 |
| 设置（我的） | 账号、标签、番茄钟设置等 | **部分实现**：`SettingsPage`支持改昵称、改密码、标签管理入口、番茄钟设置入口、成就区块、退出登录 | AI相关设置项待完成；成就与后端数据若需持久化则对接 API |
| 任务详情 / 编辑 | 详情与编辑同页、类型切换、子任务等 | **已实现**：`TaskDetailPage` 支持新建（服务端创建草稿任务）与编辑、类型切换、子任务、保存/删除等 | - |
| 番茄钟 | 计时、白噪音、杀后台结束等 | **部分实现**：`PomodoroPage` 支持创建并使用番茄钟 | 白噪音BGM模块素材导入待完成 |
| AI 助手（Agent） | 对话式辅助：查询、草稿、冲突、删除审批等 | **部分实现**：`AgentChatPage`（`/agent`），主 Tab 顶栏入口；支持基本对话和skill调用（创建任务草稿、删除任务、查询任务等） | 计划实现语音多模态输入；计划支持长期任务规划skill（后续视项目整体进度和实现效果调整） |


---

## 三、项目主要模块设计

### 3.0 模块概览
- **前端**：Flutter + Riverpod + go_router；HTTP 经 `ApiClient` 访问 `API_BASE_URL` 下的 `/api/v1/...`。
- **后端**：Django + DRF + SimpleJWT；任务域为 `tasks` 应用（多态 Task + block/ddl/todo 扩展表）；Agent 为独立 `agent` 应用，挂载于 `/api/v1/agent/`。
- **Agent**：LangGraph 编排 + 可注册 **Skill**（`registry.py`）+ LLM JSON 决策；危险操作（如删除）走 **审批表 + 独立 approve API**，前端展示「批准/拒绝」。

### 3.1 前端模块（Flutter）

**整体架构**

- `lib/core/`：主题、路由、API 客户端、通用 UI（如错误弹窗）。
- `lib/domain/`：领域模型（任务、标签、状态等）。
- `lib/data/`：仓库与 JSON 映射、Riverpod `providers`。
- `lib/features/`：按功能分区的页面（`home`、`task_list`、`calendar`、`settings`、`task_detail`、`pomodoro`、`auth`、`agent_chat`、`shell` 等）。
- `lib/shared/widgets/`：可复用小组件。

**功能划分（与交互逻辑）**

- **Shell（`MainShell`）**：底部导航 + 中间 FAB 新建任务（跳转 `/task/new`）。
- **数据层**：`TaskRepository` 等封装 REST，与 UI 通过 Provider 解耦。
- **Agent**：对话页调用 `AgentRepository` → `POST .../agent/sessions/` 与 `.../messages/`；审批调用 `.../approvals/<id>/approve|reject/`；成功后在会话列表追加助手消息。

**模块间交互（示意）**

```text
UI(各 Page) → Riverpod → Repository → ApiClient → Django REST (/api/v1/...)
AgentChatPage → AgentRepository → /api/v1/agent/*
TaskDetailPage → TaskRepository → /api/v1/tasks/*
```

### 3.2 后端模块（Django）

**整体架构**

- `config/`：工程配置、根路由（`/api/v1/auth/`、`/api/v1/agent/`、`/api/v1/` 任务域）。
- `accounts/`：注册、登录、刷新 token 等。
- `tasks/`：用户、标签、任务（block/ddl/todo）、子任务、专注会话等模型与 REST 视图。
- `agent/`：会话与消息、LangGraph 图、Skill 注册表、工具实现、审批请求模型与审批接口。

**功能划分**

- **任务域**：任务 CRUD、完成/取消/延期/子任务等（与 `api.md` 描述一致的部分由视图实现）。
- **Agent 域**：会话生命周期、单次对话 `graph.invoke`、Skill 执行、删除类 **仅创建 `ApprovalRequest`**，批准后由视图同步执行删除逻辑。

**模块间交互（示意）**

```text
Flutter → JWT → tasks.views / agent.views
agent.graph → registry.run_skill / ORM(Task) → 返回结构化 response JSON
agent.ApprovalApproveView → 校验用户 → run_skill(delete_task) → 真正删除
```

---

## 四、项目问题梳理与解决思路

### 4.1 已遇到的问题（示例）

| 问题 | 现象 / 原因 | 解决思路 |
|------|----------------|-----------|
| Agent 相关表未迁移 | `no such table: agent_sessions` 等 | 为 `agent` 应用维护 migrations，部署/本地执行 `migrate`；CI 中增加 migrate 检查或启动前自检 |
| LLM 鉴权失败导致接口 500 | OpenAI SDK 抛 `AuthenticationError` | 在 `llm` 层捕获异常并返回结构化失败；视图层兜底，避免 500；配置项放在 settings 并避免将密钥提交到版本库 |
| 端口无权限绑定 | 本机提示无法访问某端口 | 换高端口运行 dev server；检查端口占用与 Windows 排除端口范围 |
| Agent 图「空 response」误判 | 曾用「任意 dict」短路导致第二轮不推理 | 仅以 `response.type` 属于终态集合时短路；工具执行后清空非终态 `response` 并配合条件边 |

### 4.2 后续潜在问题与应对

| 潜在问题 | 说明 | 应对措施 |
|----------|------|----------|
| LLM 输出格式不稳定 | JSON 解析失败、漏字段 | 加强 schema 校验；失败时重试或降级为固定提示；关键路径用规则校验参数 |
| ReAct 多轮工具循环不可控 | 模型反复选 tool | 设置最大轮次；对「已拿到 last_tool」强制进入 compose；记录 scratchpad 便于审计 |
| 删除/高危误操作 | 模型选错 task_id | 多命中时先 `query_result` 让用户点选；审批摘要展示标题与时间；服务端再次校验 `task_id` 归属 |
| 新建任务「草稿」已落库 | 当前新建走 `POST /tasks/` 创建占位再 PATCH | 若产品要求「Agent 不落库草稿」，改为服务端草稿表或纯前端态，保存时再创建 |
| 任务列表/日历与后端能力 gap | 页面仍为占位 | 按 `api.md` 分阶段对接查询参数、分页与筛选；日历单独排期数据源 |
| 密钥与配置安全 | settings 明文 key 泄露风险 | 使用环境变量或密钥管理服务；`.gitignore` 忽略本地覆盖配置；生产禁用 DEBUG |

---

## 五、后续工作建议（非必须，供规划）

- 任务列表、日历从占位到完整数据与交互闭环；
- Agent：语音、流式回复、会话历史、更多 Skill（完成、延期、批量等）与统一审计日志；
- 番茄钟与提醒与设计稿一致的产品化打磨。

---

*文档生成位置：`中期汇报.md`（项目根目录）。*
