# 日程管理软件设计稿

该日程管理软件主要包含“任务”、“日历”两大模块，并结合番茄钟辅助功能。任务分为以下三类：block为有固定占用时间的任务；ddl为仅有终止时间的任务；todo可选包含数个子任务or预期投入时长。三类任务均可结合番茄钟提供总专注时长统计。三类任务均可赋予标签（学习、娱乐、等等，用户可自定义有哪些标签）

## 一、主页面

登录页面，进入APP时启动一个登陆页面

所有主页面下方有一个底边导航栏提供主页面之间的切换，底边栏中间为一个突出的圆形加号按钮，用于新建任务。

1. 主页（最近任务）
    （1）该页面最上方板块按照时间顺序显示今天和明天的任务（按时间排序），用户可点击查看详情。每个任务条右侧有两个按钮，一个“开始专注”启动番茄钟，一个“标记完成”直接将任务标记完成

    （2）第二个板块为统计板块。任务统计板块左侧以大字显示本周已完成任务数，右侧小字显示待完成任务数，已取消任务数，已超时任务数，以及本周完成最多的任务标签。专注统计板块的页面设计类似于“屏幕使用时间/睡眠时间”用彩色柱状图统计每天在不同标签任务上的番茄钟专注时间。

    （3）第三个板块包含所有Todo任务，按照最近使用频率排序，用户可以点击任务栏展开子任务，并勾选checkbox完成子任务。任务条右侧仅有一个“开始专注”按钮（若子任务全部完成，则添加标记完成按钮）


2. 任务列表
    总页面设计仿照外卖/购物平台网站，顶边栏提供“筛选/排序”按键入口，以及一个搜索框，左侧为一个窄侧边栏，可选类别（block，ddl，todo）。页面中心罗列各个任务条，可点击查看详情。对不同类别任务，提供不同筛选/排序机制（筛选主要针对任务标签；排序对于ddl根据截止时间排序，todo可根据已花费时长/截止时间排序）

3. 日历
   分日、周、月三个视图，可切换，切换有流畅动画，三个页面均可仿照主流软件（见截图）
   
4. 设置
   设置账号密码，管理备选标签项。

## 二、附属页面

1. 任务详情/任务编辑
    详情页和编辑页共享同一个ui，包含各个属性项的设置（注：不同类别任务的详情页会有些许不同），页面下方提供三个按键：“编辑/保存”（切换模式）、AI快捷设置（启动语音输入，自动生成）、删除任务

2. 番茄钟
   一个页面简洁的计时器，要能够锁定屏幕，仅提供一个结束按钮，支持白噪音bgm播放（预设，用户不可导入），用户杀后台自动结束计时

3. 提醒弹窗
   计时触发。若计时结束时打开app，则直接弹出一个半屏高弹窗，显示任务基本信息，并提供“启动番茄钟”、“标为完成”、“稍后完成”、“任务延期”几个选项。
   若计时结束时未启动app，本地弹出通知，点击通知可进入app
   

flutter 
python django